{% extends 'MyTreneoBundle::layout.html.twig' %}

{% block title %}Treneo - karta ogłoszenia{% endblock %}

{% block content %}
Ogłoszenie:
    <p>{{ offer.name }}</p>
    <p>{{ offer.surname }}</p>
    <p>{{ offer.phone }}</p>
    <p><a href="http://{{ offer.site }}">{{ offer.site }}</a></p>

{{ render(controller("MyTreneoBundle:Map:view", {"lat":offer.lat, "lang":offer.lang})) }}
{{ render(controller("MyTreneoBundle:Comment:list", {"offer":offer})) }}
{{ render(controller("MyTreneoBundle:Comment:form", {"offer_id":offer.id})) }}
{% endblock %}