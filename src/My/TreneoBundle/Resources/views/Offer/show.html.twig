{% extends 'MyTreneoBundle::layout.html.twig' %}

{% block title %}Treneo - karta ogłoszenia{% endblock %}

{% block content %}
Ogłoszenie:
    <p>{{ offer.name }}</p>
    <p>{{ offer.surname }}</p>
    <p>{{ offer.phone }}</p>
    <p><a href="http://{{ offer.site }}">{{ offer.site }}</a></p>

{% render "MyTreneoBundle:Map:view" with {"offer":offer} %}
{% render "MyTreneoBundle:Comment:list" with {"offer":offer} %}
{% endblock %}